# AV Bible - Cloudflare Workers static assets deployment
# Serves deploy_bundle/ as a static site:
#   web/ app at /web/, bibles JSON at /bibles/<CODE>/, visuals at /visuals/
name = "av-bible"
compatibility_date = "2026-01-01"

[assets]
directory = "./deploy_bundle"
